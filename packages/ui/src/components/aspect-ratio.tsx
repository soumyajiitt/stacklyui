"use client";

import { AspectRatio as AspectRatioPrimitive } from "radix-ui";

/** Constrains content to a given width/height ratio (wraps Radix AspectRatio). */
export const AspectRatio = AspectRatioPrimitive.Root;
